<view class="suitable">
  <view class="suitable-card-bg">
    <view class="suitable-card">
      <image class="banner" src="{{avatarUrl}}" mode="widthFix"  />
      <view class="movable-area">
        <view
          class="movable-view"
          catchtouchstart="onTouchStart"
          catchtouchmove="onTouchMove"
          catchtouchend="onTouchEnd"
          style="right: {{right}}px; bottom: {{bottom}}px"
        
        >
          <text>{{tipsText}}</text>
           <image src="{{qrcodeUrl}}" class="qrcode" mode="widthFix" />
        </view>
      </view>
    </view>
  </view>

  <view class="suitable-tips">
    我是自动向左滚动说明书，你碰我就会停下
  </view>

  <view class="suitable-btns">
    <view
      class="btn-replace"
      hover-class='btn-hover'
      bindtap="onReplace"
    >更换头像</view>
    <view
      class="btn-preview"
      hover-class='btn-hover'
      bindtap="onPreview"
    >预览</view>
  </view>
</view>


<canvas style="width: 300px; height: 400px;" canvas-id="canvas"></canvas>
